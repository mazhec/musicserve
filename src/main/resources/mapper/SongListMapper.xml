<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mazc.music.dao.SongListMapper">
    <resultMap id="BaseResultMap" type="com.mazc.music.domain.SongList">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="pic" jdbcType="VARCHAR" property="pic"/>
        <result column="style" jdbcType="VARCHAR" property="style"/>
	</resultMap>

    <resultMap id="ResultMapWidthBLOBs" type="com.mazc.music.domain.SongList" extends="BaseResultMap">
        <result column="introduction" jdbcType="LONGVARCHAR" property="introduction"/>
    </resultMap>

    <sql id="Base_Column_List" >
        id, title, pic, style
    </sql>
    
    <sql id="Blob_Column_List">
        id, title, pic, style, introduction
    </sql>

<!--    <insert id="insert" parameterType="com.mazc.music.domain.SongList" >-->
<!--        &lt;!&ndash;-->
<!--          WARNING - @mbggenerated-->
<!--          This element is automatically generated by MyBatis Generator, do not modify.-->
<!--        &ndash;&gt;-->
<!--        insert into song_list (id, singer_id, name,-->
<!--        introduction, create_time, update_time,-->
<!--        pic, lyric)-->
<!--        values (#{id,jdbcType=INTEGER}, #{singerId,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR},-->
<!--        #{introduction,jdbcType=VARCHAR}, #{createTime,jdbcType=TIMESTAMP}, #{updateTime,jdbcType=TIMESTAMP},-->
<!--        #{pic,jdbcType=VARCHAR}, #{lyric,jdbcType=LONGVARCHAR})-->
<!--    </insert>-->

    <insert id="insert" parameterType="com.mazc.music.domain.SongList">
        insert into song_list
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="title != null">
                title,
            </if>
            <if test="pic != null">
                pic,
            </if>
            <if test="style != null">
                style,
            </if>
            <if test="introduction != null">
                introduction,
            </if>
        </trim>
        <trim prefix=" values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id},
            </if>
            <if test="title != null">
                #{title},
            </if>
            <if test="pic != null">
                #{pic},
            </if>
            <if test="style != null">
                #{style},
            </if>
            <if test="introduction != null">
                #{introduction},
            </if>
        </trim>
    </insert>

    <update id="update" parameterType="com.mazc.music.domain.SongList">
        update song_list
        <set>
            <if test="title != null">
                title = #{title},
            </if>
            <if test="introduction != null">
                introduction = #{introduction},
            </if>
            <if test="pic != null">
                pic = #{pic},
            </if>
            <if test="introduction != null">
                introduction = #{introduction},
            </if>
            <if test="style != null">
                style = #{style},
            </if>
        </set>
        where id = #{id}
    </update>
    
    <delete id="delete" parameterType="java.lang.Integer">
        delete from song_list where id = #{id}
    </delete>

    <select id="selectById" resultMap="ResultMapWidthBLOBs" parameterType="java.lang.Integer">
        select 
        <include refid="Blob_Column_List" />
        from song_list
        where id = #{id}
    </select>

    <select id="allSongList" resultMap="ResultMapWidthBLOBs">
        select
        <include refid="Blob_Column_List"></include>
        from song_list
    </select>
    
    <select id="songListOfTitle" resultMap="ResultMapWidthBLOBs" parameterType="java.lang.String">
        select
        <include refid="Blob_Column_List"></include>
        from song_list
        where title = #{title}
    </select>

    <select id="likeTitle" resultMap="ResultMapWidthBLOBs" parameterType="java.lang.String">
        select
        <include refid="Blob_Column_List"></include>
        from song_list
        where title like #{title}
    </select>

    <select id="likeStytle" resultMap="ResultMapWidthBLOBs" parameterType="java.lang.String">
        select
        <include refid="Blob_Column_List"></include>
        from song_list
        where style like #{style}
    </select>
</mapper>